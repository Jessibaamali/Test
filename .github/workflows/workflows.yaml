name: build project

on:
  push:
    branches:
      -main
      
jobs:
  buildjob:
    runs-on: ubuntu-latest
    steps: 
    - name: buildstep1
      uses: actions/checkout@v4
  buildjob2:
    runs-on: ubuntu-latest
    steps:
    - name: buildstep1
      uses: actions/checkout@v4
